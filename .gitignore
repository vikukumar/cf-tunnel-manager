# ─── Go ──────────────────────────────────────────────────────────────────────
bin/
*.exe
*.test
*.out
coverage.html
coverage.out

# ─── Frontend ────────────────────────────────────────────────────────────────
web/node_modules/

# The real Vite build output is regenerated by `make build-web`.
# Keep the placeholder index.html that already exists in this path.
cmd/server/web/dist/
!cmd/server/web/dist/index.html

# ─── Config (contains secrets) ───────────────────────────────────────────────
# The sample config.yaml is intentionally committed so developers have a template.
# If you add configs/config.local.yaml or configs/config.prod.yaml, ignore them:
configs/config.local.yaml
configs/config.prod.yaml

# ─── Editor / OS ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# ─── Environment files ───────────────────────────────────────────────────────
.env
.env.local
.env.*.local

config.yaml

configs/config.yaml